{
  "compilerOptions": {
    "target": "ESNext",
    "lib": ["ESNext", "DOM"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "moduleDetection": "force",
    // "vite/client" declares *.png as modules for frame packages; "node" is
    // for the packaging CLI, which is a Node program living under
    // packages/frame-kit/src/cli.
    "types": ["vite/client", "node"],
    "skipLibCheck": true,
    "noEmit": true,

    /* Must match Card Anvil's tsconfig.app.json: the app type-checks these
       files transitively, so anything laxer here becomes a surprise error
       downstream. */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedSideEffectImports": true,
    "noUncheckedIndexedAccess": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["packages/*/src"],
  // frame-studio is a React app: it needs `jsx` and DOM-only libs, so it
  // carries its own tsconfig and is type-checked by its own `typecheck`
  // script (see the root "typecheck").
  "exclude": ["packages/frame-studio"]
}
