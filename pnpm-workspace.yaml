packages:
  - "packages/*"

# esbuild ships a platform binary and needs its postinstall to run; Vite and
# Vitest depend on it. Everything else stays unapproved by default.
allowBuilds:
  esbuild: true
